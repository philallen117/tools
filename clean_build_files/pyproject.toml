[project]
name = "clean-build-files"
version = "0.1.0"
description = "Clean build files using .cleanignore patterns"
requires-python = ">=3.13"
dependencies = ["pathspec"]

[project.scripts]
clean-build-files = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
